spring:
  application:
    name: java_spbstu
  profiles:
    default: db
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:task_db;DB_CLOSE_ON_EXIT=FALSE
    username: h2
    password: h2
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate.ddl-auto: update
